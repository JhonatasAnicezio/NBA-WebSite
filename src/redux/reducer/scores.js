const INITIAL_STATE = {
  date: '',
  events: [],
  leagues: [],
  season: {},
};

const scores = ( state =  INITIAL_STATE, action ) => {
  switch(action.type) {
  default:
    return state;
  }
}

export default scores;